import { NextProcessPipe } from './next-process.pipe';

describe('NextProcessPipe', () => {
  it('create an instance', () => {
    const pipe = new NextProcessPipe();
    expect(pipe).toBeTruthy();
  });
});
